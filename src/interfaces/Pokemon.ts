export interface PokemonData {
  name: string
}
export interface PokeCardProps {
  name: string;
  number: string;
}
export interface PokeStat {
  name: string,
  height: number,
  weight: number,
  image: string
}